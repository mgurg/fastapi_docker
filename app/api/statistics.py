# Failures per machine

# SELECT events.resource_uuid, count(events.id) from piekarnia__cukiernia_bonczyk_edaf2378f5124ca490f42984ceac235e.events where action ='issue_add'
# group by events.resource_uuid;
